---
const { image } = Astro.props;
---

<div class="relative md:flex-row px-8 gap-8 items-center h-auto">
  <div class="absolute inset-0 brightness-[20%]">
    <img
      src={image}
      class="object-cover h-full w-full"
    />
  </div>
  <div class="flex flex-col items-center w-full h-full z-10 relative">
    <slot />
  </div>
</div>
